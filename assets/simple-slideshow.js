// minimal init (keeps file module-scoped for cache busting if needed)
export default function initSimpleSlideshow() {
  // placeholder — your theme's slideshow component handles behavior
  // left intentionally empty so we can add small enhancements later if required
}
document.addEventListener('DOMContentLoaded', () => initSimpleSlideshow());